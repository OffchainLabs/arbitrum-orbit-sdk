import { PredeployInput } from './createGenesisBuilderTypes';

/**
 * Returns the default set of predeploys for Arbitrum chains
 * @returns Array of default predeploy configurations
 */
export function getDefaultPredeploys(): PredeployInput[] {
  // TODO: Populate with actual default predeploys
  // This is a placeholder that returns an empty array
  return [];
}
